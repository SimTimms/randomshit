import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  actionInputWrapper: {
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
    flexWrap: 'nowrap',
    flexDirection: 'row',
    width: '100%',
    marginTop: 10,
  },
});
